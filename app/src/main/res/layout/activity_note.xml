<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="16dp">

    <EditText
        android:id="@+id/nameNoteEditText"
        android:layout_width="238dp"
        android:layout_height="48dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:ems="10"
        android:hint="имя"
        android:inputType="textPersonName"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/CreateNoteButton"
        android:layout_width="120dp"
        android:layout_height="59dp"
        android:layout_marginTop="12dp"
        android:layout_marginEnd="24dp"
        android:text="сделать замтеку"
        app:layout_constraintBottom_toBottomOf="@+id/nameNoteEditText"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="1.0"
        app:layout_constraintStart_toEndOf="@+id/nameNoteEditText"
        app:layout_constraintTop_toTopOf="@+id/nameNoteEditText"
        app:layout_constraintVertical_bias="1.0" />

    <EditText
        android:id="@+id/textEditText"
        android:layout_width="372dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:ems="10"
        android:gravity="start|top"
        android:hint="Текст заметки"
        android:inputType="textMultiLine"
        app:layout_constraintEnd_toEndOf="@+id/CreateNoteButton"
        app:layout_constraintStart_toStartOf="@+id/nameNoteEditText"
        app:layout_constraintTop_toBottomOf="@+id/nameNoteEditText" />

</androidx.constraintlayout.widget.ConstraintLayout>